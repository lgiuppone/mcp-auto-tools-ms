apiVersion: v1
kind: Service
metadata:
  name: mcp-server-crd
  labels:
    app: mcp-server-crd
spec:
  type: ClusterIP
  selector:
    app: mcp-server-crd
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
